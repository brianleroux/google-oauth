{
  "name": "begin-app",
  "version": "1.0.0",
  "description": "A Begin app with the minimum necessary file structure",
  "scripts": {
    "start": "sandbox",
    "lint": "eslint src --fix"
  },
  "dependencies": {
    "@architect/sandbox": "^1.5.8",
    "eslint": "^6.8.0"
  }
}
